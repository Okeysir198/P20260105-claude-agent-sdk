"""Main Agent for {agent_name}.

Purpose: Handle the entire conversation workflow.
Tools: end_call, schedule_callback, and custom tools

This is the single entry point for the voice agent.
All conversation logic is handled by this agent.
"""

from .base_agent import BaseAgent


class MainAgent(BaseAgent):
    """Main agent - handles the entire conversation workflow."""

    async def on_enter(self) -> None:
        """Initialize agent and prompt first response."""
        await super().on_enter()
