# Prompt Version Registry
# Tracks all prompt versions with metadata and metrics

versions:
  v1:
    description: "Baseline version"
    created: "{created_date}"
    status: active
  # v2:
  #   description: "Empathetic approach"
  #   created: "2025-01-15"
  #   status: testing

# Default versions for each prompt file
# Format: prompt_file_name: version
defaults:
{prompt_defaults}

# Metrics tracking for A/B testing
# Updated by eval framework after test runs
metrics:
{prompt_metrics}
