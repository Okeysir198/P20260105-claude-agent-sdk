# ==============================================================================
# API PROVIDER CONFIGURATION
# ==============================================================================
# Claude API (Official Anthropic API)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Alternative providers (optional)
# ZAI_API_KEY=your-zai-api-key
# ZAI_BASE_URL=https://api.z.ai/api/anthropic
# MINIMAX_API_KEY=your-minimax-api-key
# MINIMAX_BASE_URL=https://api.minimax.io/anthropic

# ==============================================================================
# AUTHENTICATION
# ==============================================================================
# API Key (REQUIRED) - used for REST API auth and JWT secret derivation
# Generate with: openssl rand -hex 32
API_KEY=change-me-use-strong-key

# NOTE: JWT secret is derived from API_KEY using HMAC-SHA256
# No separate JWT_SECRET_KEY needed

# ==============================================================================
# USER AUTHENTICATION (REQUIRED for default users)
# ==============================================================================
# Passwords for default users created on startup
# Users are stored in SQLite database (data/users.db)
# Each user is only created if their password is set
CLI_USERNAME=admin
CLI_PASSWORD=your-admin-password-here
CLI_TESTER_PASSWORD=your-tester-password-here

# ==============================================================================
# SERVER CONFIGURATION
# ==============================================================================
CORS_ORIGINS=http://localhost:7002,https://claude-agent-sdk-chat.tt-ai.org
API_HOST=0.0.0.0
API_PORT=7001

# ==============================================================================
# OPTIONAL SETTINGS
# ==============================================================================
# Token expiration
# ACCESS_TOKEN_EXPIRE_MINUTES=30
# REFRESH_TOKEN_EXPIRE_DAYS=7

# Storage directory (defaults to backend/data)
# DATA_DIR=/path/to/data
