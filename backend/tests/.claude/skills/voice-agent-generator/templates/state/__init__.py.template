"""
Modular state management for {agent_name}.

This package provides clean, type-safe state management:
- types: Enumeration types (PersonType, CallOutcome, WorkflowStep, etc.)
- profile: Immutable Profile dataclass (frozen=True)
- session: Mutable SessionState dataclass
"""

from .types import (
    PersonType,
    CallOutcome,
    WorkflowStep,
)
from .profile import Profile
from .session import SessionState

__all__ = [
    # Profile (immutable)
    "Profile",
    # Session (mutable)
    "SessionState",
    # Types
    "PersonType",
    "CallOutcome",
    "WorkflowStep",
]
