"""Sub-Agents Package for {agent_name}.

Agent Flow:
{agent_flow_description}

Each agent inherits from BaseAgent which provides:
    - Chat context preservation during handoffs
    - Silent handoff mechanism
    - Shared userdata access
"""

# Import base components first (no circular deps)
from .base_agent import (
    BaseAgent,
    RunContext_T,
    CHAT_CONTEXT_MAX_ITEMS,
)

# Import agent classes
# from .agent01_greeting import GreetingAgent
# from .agent02_verification import VerificationAgent
{agent_class_imports}

# Import factory functions and AGENT_CLASSES
from .factory import create_agents, AGENT_CLASSES

__all__ = [
    # Base
    "BaseAgent",
    "RunContext_T",
    "CHAT_CONTEXT_MAX_ITEMS",
    # Factory
    "create_agents",
    "AGENT_CLASSES",
    # Agent Classes
{agent_class_exports}
]
